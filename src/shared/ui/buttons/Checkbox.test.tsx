describe("Checkbox component", () => {
  // const methods = useForm<AllItemsFilters>();
  it("renders checkbox with text", () => {
    // const checkbox = render(
    //   <FormProvider {...methods}>
    //     <Checkbox isChecked={false} name='country' value='AD' text='test' />
    //   </FormProvider>,
    // );
    // expect(checkbox.getByText("test")).toBeInTheDocument();
  });
});
